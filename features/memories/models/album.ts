export type AlbumCard = {
  id: string;
  name: string;
  thumbnail: string;
  count: number;
  updatedAt: string;
};

export type AlbumDescription = {
  id: string;
  title: string;
  createdAt: string;
};
