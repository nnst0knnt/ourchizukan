export type AlbumCard = {
  id: string;
  name: string;
  thumbnailUrl: string;
  count: number;
  createdAt: string;
};

export type AlbumDescription = {
  id: string;
  title: string;
  createdAt: string;
};
